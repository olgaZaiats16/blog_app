import React from 'react';
import Header from './Header';

const Archive = () => {
      return (
            <div>
                  <Header />
            </div>
      );
};

export default Archive;