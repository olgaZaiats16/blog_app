import React from 'react';
import Header from './Header';

const Category = () => {
      return (
            <div>
                  <Header />
            </div>
      );
};

export default Category;